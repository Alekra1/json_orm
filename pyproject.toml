[project]
name = "json-orm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "coverage>=7.13.5",
    "flake8>=7.3.0",
]
